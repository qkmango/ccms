<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.qkmango.ccms.mvc.dao.StatisticDao">

    <select id="count" resultType="int">
        SELECT FOUND_ROWS()
    </select>

    <select id="ConsumeCountPriceByDayAndType" resultType="cn.qkmango.ccms.domain.vo.statistic.ConsumePriceCount">
        select
        DATE_FORMAT(createTime, '%Y-%m-%d') AS date,
        sum(price)                          AS price,
        count(id)                           AS count,
        type
        from t_consume
        <where>
            type not in ('PAYMENT','RECHARGE','REFUND')
            <if test="startTime != null">
                and createTime &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                and createTime &lt;= #{endTime}
            </if>
        </where>
        group by DAY(createTime),type
        order by date;
    </select>

</mapper>
